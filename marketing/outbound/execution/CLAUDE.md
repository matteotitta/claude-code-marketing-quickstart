# marketing/outbound/execution/ — produce, write, ship

The actual artifacts you ship. Final copy, sequences, ads, articles.

Outputs from execution-style skills land here (e.g., `/aeo-content` outputs go in `content/execution/`, `/email-nurture` sequences go in `lifecycle/execution/`).
