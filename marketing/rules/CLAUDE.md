# marketing/rules/ — your project guardrails

This folder holds your **project-specific rules**: when Claude should ask humans vs ship autonomously (`escalation.md`), review gates per output type (`gate-rules.md`).

**To populate:** hand-edit the two files based on your team's review discipline. Default versions in the example are sensible starting points.

**For the format:** see [`../../pulse-analytics-example/rules/`](../../pulse-analytics-example/rules/).
